---
orphan: true
nosearch: true
myst:
  html_meta:
    robots: noindex, follow
---

# Página no encontrada

La página que buscas no existe o ha sido movida.

Te invito a leer alguno de los siguientes artículos:

```{postlist} 5
:date: %Y-%m-%d
:format: {date} - {title}
:excerpts:
```

<script>
(function () {
  "use strict";
  var path = window.location.pathname.replace(/\/+$/, "").toLowerCase();
  var lookup = path + "/";

  fetch("/redirects.json")
    .then(function (r) { return r.ok ? r.json() : {}; })
    .then(function (map) {
      /* 1. Try redirect map (legacy blog post paths) */
      if (map[lookup]) {
        window.location.replace(map[lookup]);
        return;
      }

      /* 2. Detect _sources pattern → canonical post */
      var srcMatch = path.match(/^\/_sources\/(.+)\.rst(?:\.txt)?$/);
      if (srcMatch) {
        window.location.replace("/" + srcMatch[1] + "/");
        return;
      }

      /* 3. Detect tag/category pattern → search */
      var match = path.match(/\/(?:category|tag)\/([^/]+)/);
      if (match) {
        var query = decodeURIComponent(match[1]).replace(/-/g, " ");
        window.location.replace("/search/?q=" + encodeURIComponent(query));
        return;
      }

      /* 4. Default: recent posts already visible */
    })
    .catch(function () {
      /* 4. Default: recent posts already visible */
    });
})();
</script>
